import XCTest
@testable import weipl_checkout_ios

final class weipl_checkout_iosTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(weipl_checkout_ios().text, "Hello, World!")
    }
}
