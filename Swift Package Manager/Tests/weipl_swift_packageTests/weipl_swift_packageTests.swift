import XCTest
@testable import weipl_swift_package

final class weipl_swift_packageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(weipl_swift_package().text, "Hello, World!")
    }
}
